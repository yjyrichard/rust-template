// src/main.rs - 程序入口文件
// 每个 Rust 可执行项目都必须有一个 main 函数作为程序的起点
// `fn` 是 Rust 中定义函数的关键字

fn main() {
    // println! 是一个宏（macro），注意末尾的感叹号 !
    // 宏和函数的区别：宏在编译时展开，可以接受可变数量的参数
    // 类似于 C 的 printf 或 Python 的 print
    println!("Hello, world!");
}
